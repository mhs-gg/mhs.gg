// import bcrypt from 'bcrypt';

/**
 *
 * @param {input} - The password from the input form
 * @param {storedPassword} - The password from the database
 * @description Tests the password from the input form to the password stored in the database
 *
 * @returns Boolean
 */
// export const passwordsMatch = async (input: string, storedPassword: string) => {
//   return await bcrypt.compare(input, storedPassword);
// };